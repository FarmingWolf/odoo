<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="meeting_minutes_type_view_tree" model="ir.ui.view">
        <field name="name">meeting.minutes.type.view.tree</field>
        <field name="model">meeting.minutes.type</field>
        <field name="arch" type="xml">
            <tree string="meeting_minutes_type_tree" create="True" edit="True" delete="True" import="False" editable="bottom" open_form_view="True">
                <field name="sequence" widget="handle"/>
                <field name="name"/>
            </tree>
        </field>
    </record>

    <record id="meeting_minutes_type_view_form" model="ir.ui.view">
        <field name="name">meeting.minutes.view.form</field>
        <field name="model">meeting.minutes.type</field>
        <field name="arch" type="xml">
            <form string="meeting_minutes_type_form" create="True" edit="True" duplicate="True" delete="True">
                <group>
                    <field name="name"/>
                </group>
            </form>
        </field>
    </record>

    <record id="meeting_minutes_type_action" model="ir.actions.act_window">
        <field name="name">会议纪要类型</field>
        <field name="res_model">meeting.minutes.type</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{'search_default_active': True}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                点击“新建”按钮，定义会议纪要分类名。
            </p>
        </field>
    </record>
</odoo>